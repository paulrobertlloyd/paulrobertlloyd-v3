#!/bin/sh

set -e

echo "Generating website files and saving them to destination"
START=$SECONDS
bin/static
JEKYLL_ENV=production bundle exec jekyll build --config etc/jekyll/config.yml

echo $((SECONDS - START)) seconds
