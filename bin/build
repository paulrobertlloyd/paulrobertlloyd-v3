#!/bin/sh
set -e

echo "Generating website files and saving them to destination"
START=$SECONDS
bundle exec jekyll build --config etc/jekyll/config.yml
npm run build

echo $((SECONDS - START)) seconds
